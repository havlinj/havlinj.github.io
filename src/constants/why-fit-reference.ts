/**
 * Longest single-line clause used to size the /why column (`--why-font-scale`).
 * Must stay in sync with copy in `WhyContent.astro` and with the e2e guard below.
 */
export const WHY_FIT_REFERENCE_LINE =
  'but they say little about how someone actually thinks.';
