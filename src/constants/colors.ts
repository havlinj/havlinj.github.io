/**
 * Canonical theme colors for e2e (`getComputedStyle`) and TS consumers.
 * sync: src/styles/tokens.css
 */

export const COLOR_INK_HEX = '#111' as const;
export const COLOR_PAGE_BG_HEX = '#e0f7fa' as const;

/** Ink — body text, dark panels */
export const RGB_INK = 'rgb(17, 17, 17)';

/** Page wash (body background on default routes) */
export const RGB_PAGE_BG = 'rgb(224, 247, 250)';
